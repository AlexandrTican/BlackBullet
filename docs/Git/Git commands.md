### Git Commands


### (1) Git Commit:
# A commit in a git repository records a snapshot of all the (tracked) files in your directory
# Git can (when possible) compress a commit as a set of changes, or a "delta", from one version of the repository to the next    

### !!! COMMIT OFTEN, EVERY SMALL CHANGE